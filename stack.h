#pragma once

#define SIZE 10;

class Stack {
    private:
        int *arr;
        int top;
    public:
        stack(int size);
        void push(int x) {
            if (isFull()) {
                
            }
        }
    
        isEmpty()
}